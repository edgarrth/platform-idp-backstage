# frontend-cart-page

Pagina de carrito usada por `frontend`.

## Relacion

```txt
frontend
  -> frontend-cart-page
```

## Ubicacion

El codigo vive dentro del repo `frontend`, en la carpeta `templates/`.
