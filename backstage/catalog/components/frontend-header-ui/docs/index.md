# frontend-header-ui

Componente visual de cabecera usado por `frontend`.

## Relacion

```txt
frontend
  -> frontend-header-ui
```

## Ubicacion

El codigo vive dentro del repo `frontend`, en la carpeta `templates/`.
